spring:
  application:
    name: parish-celebrations
  datasource:
    url: jdbc:mysql://localhost:3306/parish?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: parish
    password: parish_dev
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    show-sql: false

server:
  port: 8080
  error:
    include-message: always

logging:
  level:
    com.parish.celebrations: DEBUG

app:
  jwt:
    secret: "kairos-parish-super-secret-key-for-jwt-signing-must-be-long-enough-256bits"
    expiration-ms: 86400000   # 24 hours
